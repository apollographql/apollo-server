{
  "packages": ["packages/*", "v3"],
  "version": "independent",
  "command": {
    "version": {
      "message": "Release",
      "ignoreChanges": ["**/*.md"],
      "includeMergedTags": true
    },
    "bootstrap": {
      "scope": "@apollo/server"
    },
    "publish": {
      "message": "Release"
    }
  }
}
