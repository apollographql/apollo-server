// TODO
// use mock implementations for dynamodb
// jest.mock('dynamodb', () => require('aws-sdk-mock'));

// import { DynamoDBCache } from '../src/index';
// import { testKeyValueCache } from '../../apollo-server-caching/src/__tests__/testsuite';

// testKeyValueCache(new DynamoDBCache({region: 'us-east-1',endpoint: 'http://localhost:8000'},{tableName: 'psq'}));
