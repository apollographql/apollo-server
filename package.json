{
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/apollostack/graphql-server.git"
  },
  "scripts": {
    "compile": "lerna exec -- npm run compile",
    "lint": "tslint ./packages/**/src/**/*.ts --exclude \"./packages/**/node_modules/**/*.ts\"",
    "prebootstrap": "npm install",
    "postinstall": "lerna bootstrap",
    "test": "npm run testonly --",
    "posttest": "npm run lint",
    "testonly": "mocha --reporter spec --full-trace --timeout 5000 --compilers ts:ts-node/register ./test/tests.js",
    "coverage": "nyc _mocha --full-trace --timeout 5000 ./test/tests.js",
    "pretravis": "npm run compile",
    "travis": "npm run coverage",
    "posttravis": "npm run lint",
    "check-updates": "lerna exec ./node_modules/.bin/npm-check-updates -- -u"
  },
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/mocha": "^2.2.38",
    "@types/node": "^7.0.3",
    "@types/sinon": "^1.16.34",
    "chai": "^3.5.0",
    "graphql": "^0.9.0",
    "lerna": "2.0.0-beta.38",
    "mocha": "^3.2.0",
    "npm-check-updates": "^2.8.9",
    "nyc": "^10.1.2",
    "sinon": "^1.17.7",
    "supertest": "^3.0.0",
    "supertest-as-promised": "^4.0.2",
    "ts-node": "^2.1.0",
    "tslint": "^4.3.1",
    "typescript": "^2.1.5"
  }
}
